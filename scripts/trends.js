function mobile_dims()
{
    console.clear();
    let winheight = window.innerHeight;
    let winwidth = window.innerWidth;

    if(winWidth < 768)
    {
        // document.getElementsByClassName("mobile-neumorphism")[0].setAttribute("height" , winheight);
    }
}